# ============================================================================
# BACKEND: Hybrid Session Variable Mode
# ============================================================================
# Copy to .env in sample-app/
#
# Users authenticate via an Identity Provider. All Snowflake API calls use a
# shared service PAT. The tenant claim from the IdP JWT is injected as a
# session variable in Cortex Agent requests, used with Snowflake Row Access
# Policies for per-tenant data filtering.
# ============================================================================

SERVER_PORT=3001
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000

AUTH_MODE=HYBRID
# TENANT_ISOLATION_MODE=SESSION_VAR  (this is the default)

# Thread tagging — max 8 characters (a per-user hash is appended automatically)
ORIGIN_APPLICATION=ask_crtx

# Snowflake
SNOWFLAKE_HOST=your_account.snowflakecomputing.com
SNOWFLAKE_DATABASE=your_database
SNOWFLAKE_SCHEMA=your_schema
SNOWFLAKE_PAT=your_personal_access_token

# OAuth (from your Identity Provider)
OAUTH_TOKEN_URL=https://your-idp.example.com/oauth/token
OAUTH_CLIENT_ID=your_client_id
OAUTH_CLIENT_SECRET=your_client_secret
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# IdP JWT Validation
IDP_JWKS_URL=https://your-idp.example.com/.well-known/jwks.json
IDP_ISSUER=https://your-idp.example.com

# Optional — expected "aud" claim in the JWT. If not set, OAUTH_CLIENT_ID is used
# (which matches the id_token's aud for all OIDC-compliant IdPs).
# IDP_AUDIENCE=your_audience

# Tenant Configuration
# SESSION_VAR_NAME: the variable name passed to the Cortex Agent as a session variable
# This becomes available in the Snowflake query context for Row Access Policies
SESSION_VAR_NAME=TENANT

# CLAIM_KEY: the JWT claim containing the tenant identifier (e.g., tenant, org_id)
CLAIM_KEY=tenant

# USERNAME_CLAIM_KEY: JWT claim for per-user thread scoping (default: email)
# USERNAME_CLAIM_KEY=email
